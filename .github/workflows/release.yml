name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Run tests
        run: go test -v ./...

      - name: Build binaries
        run: |
          # Build for multiple platforms
          mkdir -p dist
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o dist/swk-linux-amd64 ./cmd/swk
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o dist/swk-linux-arm64 ./cmd/swk
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o dist/swk-darwin-amd64 ./cmd/swk
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o dist/swk-darwin-arm64 ./cmd/swk
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o dist/swk-windows-amd64.exe ./cmd/swk

      - name: Create checksums
        run: |
          cd dist
          sha256sum * > checksums.txt

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/swk-linux-amd64
            dist/swk-linux-arm64
            dist/swk-darwin-amd64
            dist/swk-darwin-arm64
            dist/swk-windows-amd64.exe
            dist/checksums.txt
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
